import React from "react";
import { Landing, ReportSheet } from "../src/views";
import { NavBar } from "../src/components";

const home = () => {
  return (
    <div>
      <Landing />
    </div>
  );
};

export default home;
